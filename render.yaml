services:
  - type: web
    name: control-point-viewer
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python web/serve.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.6"
